#!/bin/bash

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

# The restic repository has been manually initialized with the following command:
#
#   restic-{{ bucket_name }} init
#
BUCKET_NAME="{{ bucket_name }}" "${DIR}/restic-with-bucket" "$@"
