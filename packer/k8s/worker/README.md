# Worker

## CIDRs

We use the same default Pod and service CIDRs as K3s (see https://docs.k3s.io/cli/server#k3s-server-cli-help[K3s server CLI help]):
- Pod CIDR: `10.42.0.0/16`
- Service CIDR: `10.43.0.0/16`
